# array, list
frase = input ("Escreva aqui uma frase\n")
palavras = frase.split()                      # parte frase em palavras
print (palavras)
for i in range(len(palavras)):              
    palavra_invertida = palavras[i][::-1]     # inverte a palavra
    print(palavra_invertida)